{
  "title": "Subnet Type",
  "description": "Deploy to internal subnets (cannot reach the internet) or private subnets (internet egress traffic allowed)",
  "type": "string",
  "enum": [
    "internal",
    "private"
  ],
  "default": "internal"
}
